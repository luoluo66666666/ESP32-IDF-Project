idf_component_register(
    SRCS
        "Wifi_manage.c"
    INCLUDE_DIRS
        "include"
    REQUIRES
        esp_wifi
        esp_event
        esp_netif
        nvs_flash
        lwip
        ctrl_protocol
        OTA
)
